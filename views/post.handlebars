{{!-- post --}}

<div class="container">
    <div class="row">
        <div class="col-md-8">
            <div class="card mb-3">
                <div class="card-header">
                    <h3>{{post.title}}</h3>
                    <p class="text-muted">Posted by {{post.user.username}} on {{formatDate post.createdAt}}</p>
                    <p class="card-text">{{post.body}}</p>
                </div>
            </div>
        
            {{#if post.comments}}

            <section class="card">
              <div class="card-header">
                <h3>Comments 📄 </h3>
              </div>

              <div class="card-body">
                {{!-- <ul class="list-group"> --}}
                    {{!-- <p class="text-muted">Posted by {{username}} on {{formatDate post.createdAt}}</p> --}}
                    {{!-- <p class="card-text">  --}}
                    <div>
                      {{#each post.comments as |comment|}}
                      {{comment.body}}
                        {{/each}}
                    </div>
              </div>
            </section>
            {{/if}}
        </div>
    </div>

  
    <div class="row">
      <div class="col-md-8">
        {{!-- <div class="card"> --}}
          <form id="new-comment-form">
            <div class="card-header" class="card mb-3">
              <h3>Add New Comment 📝</h3>
            </div>
              <input type="hidden" name="postId" value="{{post.id}}" />
            <div class="form-group">
              <textarea name="comment-body" class="form-control" rows="3" placeholder="Leave a comment"></textarea>
            </div>
            
            <button type="submit" class="btn btn-primary">Add Comment ➕</button>
          </form>
        {{!-- </div> --}}
      </div>
    </div>
</div>

<script src="/js/comments.js"></script>